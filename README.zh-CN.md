# Desklist

[English](README.md) | 中文

Windows 桌面待办/提醒小工具。无边框桌面底层窗口，最小化到系统托盘，支持定时提醒和周期事件。

Desklist 面向想要“抬手就记、随时可见”的 Windows 用户。你可以快速打开它、立刻记录待办、把它安静地放在系统托盘里，并通过定时提醒和周期事件维持稳定的日常节奏，而不必切到复杂笨重的项目管理软件。

相比功能臃肿的待办应用，Desklist 更强调桌面原生体验：简洁的毛玻璃界面、列表与日历双视图、本地 SQLite 存储、轻量提醒能力，以及更贴近日常工作的使用方式。

## 功能

- 事件创建、编辑、删除，支持标题、描述、时间
- 四种筛选视图：今天 / 未完成 / 已完成 / 全部
- **日历视图**：标题栏切换按钮在列表与日历视图间切换；有未完成事项的日期显示圆点；点击日期查看当天事项
- 定时提醒：到期提醒 + 可选提前提醒（5/15/30/60 分钟或 1 天前）
- 周期事件：每天、每周、每月，支持设置截止时间
- 完成周期事件后自动生成下一次
- 系统托盘常驻，关闭窗口可隐藏到托盘
- 单实例运行
- 深色毛玻璃 UI（Windows Acrylic）
- 首次启动可选择任务数据保存位置，不再强制占用系统盘
- 设置页可查看并打开当前数据文件夹

## 数据保存

首次启动时，Desklist 会让你选择任务数据保存在哪个文件夹。如果电脑中已经存在旧版任务，程序会先安全复制并检查完整，再启用新位置；原来的数据库不会自动删除。

软件升级或重新安装不会覆盖你选择的数据文件夹。请不要在 Desklist 运行时手动移动或修改 `desklist.db`、`desklist.db-wal` 和 `desklist.db-shm`。

## 技术栈

- 前端：Vue 3 + TypeScript + Vite
- 桌面框架：Tauri 2
- 数据存储：SQLite（Rust `sqlx` 统一管理）
- 提醒调度：Rust + Tokio
- 通知：`tauri-plugin-notification`

## 开发

```bash
# 安装依赖
npm install

# 开发模式
npm run tauri dev

# 生产构建
npm run tauri build
```

## 开发路线

下一阶段将把 Desklist 升级为浏览器个人工作台，再增加 Agent 接口。已完成内容、开发顺序和暂不开发范围见[个人工作台路线图](docs/ROADMAP.zh-CN.md)。

## 更新日志

### 未发布

- 首次启动支持选择任务数据保存位置，并安全迁移旧数据库
- 设置页显示当前数据位置，点击可直接打开文件夹
- 任务读写、重复任务和提醒逻辑统一移入 Rust 后台
- 增加临时数据库自动测试，避免测试改动用户真实任务

### v1.5.1

- 修复窗口层级错误：Desklist 现在固定在桌面底层，可被浏览器等普通应用窗口覆盖

### v1.5.0

- 修复周期事件在“完成/未完成”反复切换时可能重复生成下一次的问题
- 修复每月重复在月末日期的进位错误（例如 1 月 31 日会正确推进到 2 月 28 日，而不是跳到 3 月）
- 修复切换完成状态时的提醒队列行为：标记完成会清理未触发提醒，恢复未完成仅重建未来提醒
- 修复窗口层级行为与产品预期不一致的问题（改为 `alwaysOnTop`）
- 修复日历选中日期后新建事件默认时间仍落在当天的问题（现默认带入所选日期）
- 修复日历中文月份/日期显示异常（例如 2026�?{...}）的问题

### v1.4.0

- 新增日历视图：标题栏切换按钮可在列表与日历视图间切换
- 有未完成事项的日期显示蓝色圆点，今日日期高亮
- 点击日期在网格下方展示当天未完成事项，支持完成、删除、编辑操作
- 点击 `<` / `>` 切换月份，圆点自动更新
- 月份标题随语言切换（中文显示「2026年3月」，英文显示「March 2026」）

### v1.3.0

- 在设置面板中新增语言切换功能（中文 / English）
- 所有界面文字即时响应语言切换，偏好设置重启后保留
- **安全：** 为 Tauri WebView 配置内容安全策略（CSP）
- **安全：** 修复 `minimatch`（ReDoS ×3）和 `rollup`（路径遍历）的高危 CVE，升级相关依赖
- **安全：** 主题色写入 CSS 属性前增加 hex 格式校验
- **安全：** 移除权限清单中未使用的 `opener` 能力
- **安全：** 标题和描述输入框增加长度限制（200 / 1000 字符）
- **安全：** `recurrence` 字段写入数据库前经白名单枚举校验
- **稳定性：** 将 Tauri setup 路径中的 `unwrap()` 改为正常错误传播

### v1.2.1

- 将列表筛选“即将”调整为“未完成”，显示全部未完成任务
- 新增设置项：可配置“新建事件时到期提醒默认开启/关闭”
- 修复多个界面中文文案乱码问题（新建事件、提醒配置、设置面板、README）

### v1.2.0

- 新增外观设置面板（标题栏齿轮按钮进入）
- 支持窗口透明度调节（10%~100%）
- 支持主题色自定义
- 设置使用 `tauri-plugin-store` 持久化

### v1.1.0

- 重构深色毛玻璃风格，启用 Windows Acrylic 窗口特效
- 统一主要组件深色主题适配

### v1.0.2

- 修复通知时间显示问题
- 改进 UI 视觉和交互细节

### v1.0.1

- 修复提醒通知无声音的问题，触发提醒时播放系统默认提示音 (#1)

### v1.0.0

- 初始发布

## 许可证

[MIT](LICENSE)

